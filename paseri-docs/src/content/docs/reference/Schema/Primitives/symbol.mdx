---
title: "Symbol"
sidebar:
    order: 6
---

import { Playground } from "../../../../../components/Playground.tsx";

## Example

```typescript
import * as p from '@vbudovski/paseri';

const schema = p.symbol();
const data = Symbol.for('foo');

const result = schema.safeParse(data);
if (result.ok) {
    // result.value typed as `symbol`.
}
```

## Playground

<Playground
    schemaDefaultValue={"```typescript\np.symbol()\n```"}
    dataDefaultValue={"```typescript\nSymbol.for('foo')\n```"}
    client:idle
/>
