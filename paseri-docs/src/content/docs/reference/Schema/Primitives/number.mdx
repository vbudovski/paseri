---
title: "Number"
sidebar:
    order: 2
---

import { Playground } from '../../../../../components/Playground.tsx'

## Example

```typescript
import * as p from '@vbudovski/paseri';

const schema = p.number();
const data = 123;

const result = schema.safeParse(data);
if (result.ok) {
    // result.value typed as `number`.
}
```

## Playground

<Playground
    schemaDefaultValue={"p.number()"}
    dataDefaultValue={"123"}
    client:idle
/>

## Validators

### `gte`

Greater than or equal to `value`.

```typescript
p.number().gte(5);
```

### `gt`

Greater than `value`.

```typescript
p.number().gt(5);
```

### `lte`

Less than or equal to `value`.

```typescript
p.number().lte(5);
```

### `lt`

Less than `value`.

```typescript
p.number().lt(5);
```

### `int`

A valid integer.

```typescript
p.number().int();
```

### `finite`

A finite number, excluding `Number.NEGATIVE_INFINITY` and `Number.POSITIVE_INFINITY`.

```typescript
p.number().finite();
```

### `safe`

A valid integer between `Number.MIN_SAFE_INTEGER` and `Number.MAX_SAFE_INTEGER`.

```typescript
p.number().safe();
```
