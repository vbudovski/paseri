---
title: "Undefined"
sidebar:
    order: 7
---

import { Playground } from "../../../../../components/Playground.tsx";

## Example

```typescript
import * as p from '@vbudovski/paseri';

const schema = p.undefined();
const data = undefined;

const result = schema.safeParse(data);
if (result.ok) {
    // result.value typed as `undefined`.
}
```

## Playground

<Playground
    schemaDefaultValue={"p.undefined()"}
    dataDefaultValue={"undefined"}
    client:idle
/>
