---
title: "Null"
sidebar:
    order: 8
---

import { Playground } from "../../../../../components/Playground.tsx";

## Example

```typescript
import * as p from '@vbudovski/paseri';

const schema = p.null();
const data = null;

const result = schema.safeParse(data);
if (result.ok) {
    // result.value typed as `null`.
}
```

## Playground

<Playground
    schemaDefaultValue={"p.null()"}
    dataDefaultValue={"null"}
    client:idle
/>
